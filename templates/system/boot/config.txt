#/boot/overlays/README for all available options

# Use with linux-rpi
# initramfs initramfs-linux.img followkernel
# start_x=1
# display_auto_detect=1
# camera_auto_detect=1

# Mainline Kernel extended firmware files
# start_file=start4x.elf fixup_file=fixup4x.dat
# cmdline=cmdline.txt

# No audio disables audio for the HDMI ports only, not other sound cards e.g camera sound
dtoverlay=vc4-kms-v3d-pi4,noaudio,cma-256
# dtoverlay=rpivid-v4l2
max_framebuffers=2
# dtoverlay=vc4-kms-v3d

# Change as needed, more than is needed is not helpful
# gpu_mem_512=128
# gpu_mem_1024=256
gpu_mem=256
cma_lwm=16
cma_hwm=32
cma_offline_start=16

# Needed for USB raid driver
dtoverlay=dwc2

# Uncomment to enable bluetooth
#dtparam=krnbt=on

# Remove if using armv6 Raspberry Pi Zero
arm_64bit=1

enable_uart=1

# PWM fan control
# dtoverlay=gpio-fan,gpiopin=18,temp=55000

# [pi4]
# Run as fast as firmware / board allows
# Doesn't appear to change max freq from 1500 to 1800
# arm_boost=1

# Overclocking options
h264_freq_min=250
arm_freq=1800
